<Window x:Class="Feest.Presentation.DayplanWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Feest.Presentation"
        mc:Ignorable="d"
       Title="Gentse feesten"  
        Width="{x:Static SystemParameters.PrimaryScreenWidth}"  
        Height="{x:Static SystemParameters.PrimaryScreenHeight}"  
        WindowState="Maximized">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <Label x:Name="UserDetails"/>
            <Label Content="Search Event"/>
            <TextBox x:Name="SearchEventTextBox" TextChanged="SearchEventTextBox_TextChanged"/>
            <Label Content="Overview event(s)" />
            <ListView x:Name="EventList" SelectionChanged="EventsList_SelectionChanged" Height="984" />
        </StackPanel>
        <TextBlock x:Name="EventInfoTextBox" />
    </Grid>
</Window>
